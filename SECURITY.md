# 安全策略

## 报告安全漏洞

**优先私密渠道**：请发邮件到仓库维护者（见 CODEOWNERS 或 README 顶部的维护者信息），
或 GitHub Security Advisory（`gh security-advisories`）。**不要**在公开 issue 中提交
真实 token、证书或可复现的密钥。

预期处理时间：高危 72 小时内确认，7 天内给出修复或缓解方案；修复发布前不会公开细节。

## 安全模型要点（详见 README）

- 客户端↔orbitd：双层 TLS（隧道层 TLS + 出口拨号层独立）；客户端证书校验 custom CA。
- 管理面默认只在 `127.0.0.1`（`admin.addr`）；对外暴露需自担风险。
- 账户独立虚拟网络，设备凭证（token）可轮换/吊销；防爆破限流内置。
- 数据面公网入口（默认 `:28443` 与 `--web-port`）：如生产对外，建议前置防火墙 + 白名单，
  与管理面（`/api/v1/admin/*`）严格隔离。

## 隐私

- 计量只聚合字节数与时间桶，不上传流量内容；出口节点对目的地可见明文属设计内行为，
  产品文档需向出口使用者明示信任边界。